{% extends 'base.html.twig' %}

{% block body %}

	<div class="container">

		<h1 class="mt-4">Création d'un ingredient</h1>
		{{ form_start(form) }}

		<div class="form-group">
			{{ form_row(form.name) }}
		</div>

		{{ form_end(form) }}
	</div>

{% endblock %}
